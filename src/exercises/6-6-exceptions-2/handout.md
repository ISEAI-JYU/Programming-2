Create a `main` method that repeatedly reads integers from the user in a loop for as long as the user enters a non-empty input. If the user enters something other than an integer, print an error message and continue reading input.

Store the numbers in an array.
Finally, print all numbers stored in the array.