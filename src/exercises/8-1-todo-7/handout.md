Return the project refactored in Exercise 8.1.
Recap of the steps in this part:

- Create a separate model object (`Task`) for tasks instead of using UI components directly.
- Add at least a task title and a done/not-done state to the model.
- Use `ObservableList<Task>` as the primary data structure for tasks.
- Update the task creation logic to use the model object.

When the phase is complete, run `git add` for the modified files and create a `git commit`.
Return your project files.