Continue the `List` class by implementing the `remove(T element)` method, which removes the first occurrence of the given element.

- If the element is found and removed, return `true`.
- If the element is not found, the list remains unchanged and the method returns `false`.

Hint: You can use the `remove(int index)` method you have already implemented. Do not duplicate the element-shifting logic in this method.