Modify the `add` method so that it adds an element to the end of the list and, if necessary, creates a new larger array (1.5× or 2× the current size) and copies the elements from the old array into the new one. Remember to update the list size appropriately.

Also implement a `size()` method that returns the current number of elements in the list.

Use the `get` and `size` methods to help test that the `add` method works correctly even when the array is full and a new array has been created.