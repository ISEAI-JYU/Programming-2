Return the project refactored in Exercise 8.2.
Recap of the steps in this part:

- Replace the task `VBox` + `CheckBox` listing with a `TableView` component.
- Add at least the following columns to the table: task (title), done status.
- Bind the table's data to an `ObservableList<Task>` list.
- Allow the user to select a task and delete the selected row.

When the phase is complete, run `git add` for the modified files and create a `git commit`.
Return your project files.