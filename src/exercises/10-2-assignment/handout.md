Create controller classes for each view.
Each event handler must do something, for example print text to the console.
At this stage, it is not yet possible to test the functionality of controllers other than the main view controller.
However, the project must not contain any compilation errors.