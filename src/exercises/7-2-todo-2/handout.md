Tee painikkeelle tapahtumankäsittelijä, joka lisää tekstikenttään kirjoitetun
tekstin `Label`-komponenttiin. Näytä `Label`-komponentissa kaikki tehtävät,
jotka on lisätty, erottamalla ne toisistaan rivinvaihdolla. 

Tee projektillesi Git-varasto, ja tee tämän tehtävän tehtyäsi siihen ensimmäinen
commit.