Create a generic method `findLargest` that finds the largest element in a list.
The list passed as a parameter must contain elements that implement the `Comparable` interface, but otherwise the list may contain elements of any type.
Do not use any of the built-in methods from the `Collections` class.